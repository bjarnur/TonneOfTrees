#version 330 core

layout(location = 0) out vec3 foo_bar;

out vec4 FragColor;

in vec2 TexCoords;

uniform sampler2D texture_diffuse1;

void main()
{    
    foo_bar = texture(texture_diffuse1, TexCoords).xyz;
	FragColor = texture(texture_diffuse1, TexCoords);
	
	//color = vec3(1.0, 0.0, 0.0);

	//FragColor = vec4(1.0, 0.0, 0.0, 1.0);
}